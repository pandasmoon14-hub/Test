# Claude Compatibility Note

This repository is **Myravant**.

It previously operated under the **Astra Ascension** identity. Historical,
provenance-bearing, frozen-evidence, and compatibility uses of that name remain
valid and must not be rewritten merely for naming consistency.

Canonical coding-agent navigation and operating guidance is in `AGENTS.md`.
Read it before performing work.

Resolve current gates, phases, and workstream status from the repository's
owning control and manifest artifacts; do not treat this compatibility note as
status authority. During identity-migration work, follow
`docs/doctrine/control/myravant_identity_migration_contract.md`.

Historical Aether Forge and extraction material is subordinate developer
tooling/history, not the repository's current identity or current work queue.
The existing `astra_runtime` software namespace is likewise a
compatibility-bearing legacy identifier pending a separately audited migration
tranche; it does not make Astra Ascension the current project identity.

If tool-specific guidance conflicts with current repository authority, the
repository authority wins.
