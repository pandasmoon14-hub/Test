# batch_b_activity_prompt_strict_v0_1

You are performing **strict Astra Ascension Batch B conversion**.

This is a high-discipline operational conversion pass. Favor boundaries, procedural clarity, and explicit unresolved status over smooth prose.

## Non-negotiable rules

- Stay entirely inside Batch B ownership.
- Do not universalize donor access models, ideology, or activity cadence.
- Do not invent missing Astra world law.
- Do not absorb schema-bearing content that belongs in Batch C.
- If the donor relies on a highly specific loop, preserve it as donor-local or optional unless Astra has already promoted it.

## Batch B ownership

Own only:
- operational anchors and return-loop grammar
- access law and operational gating
- contract / mission / trial / expedition procedure
- factions as pressure structures
- legwork, preparation, investigation, and consequence pressure
- network-access and remote-operation procedure where operationally relevant
- standing / heat / notoriety style consequence layers
- optional base-operations overlays

## Forbidden drift

Do not absorb:
- chargen or chassis logic
- item-class or actor record grammar
- full site or settlement schemas
- exact event-table catalogs
- exact asset stat blocks
- donor ideology as Astra baseline

## Cross-donor controls

Current accepted Astra-facing controls include:
- access law is real doctrine, but access instruments are not universal
- brokered contracts are a lawful pattern, not the only pattern
- optional base-operations overlays exist, but no donor cadence is mandatory
- anti-tech ideology is never baseline by default
- platform/remote procedure must remain distinct from platform record grammar

## Required decision model

For each meaningful donor construct, explicitly label one of:
- Baseline Astra mapping
- Optional Astra profile
- Donor-local retained structure
- Provisional unresolved mapping
- Doctrine escalation

## Required output format

Use exactly these headings:
1. Scope
2. Accepted Astra mappings
3. Optional Astra profiles
4. Donor-local retained structures
5. Provisional unresolved mappings
6. Doctrine escalations
7. Batch A / C handoffs
8. Final disposition

## Strict handling instructions

- If access depends on tokens, seals, credentials, routes, warrants, portals, or networks, do not flatten them together unless the shared Astra grammar is explicit.
- If the donor uses a mission-board, fixer, broker, or patron loop, preserve the grammar without forcing the donor’s specific social idiom into Astra baseline.
- If the donor relies on stronghold/capital/home-base cadence, keep it optional unless multiple donors support a broader Astra pattern.
- If the donor has network or matrix procedure, do not universalize its internal cosmology.

## Final instruction

Produce a strict Batch B conversion document that can survive later cross-donor comparison without ambiguity about what is promoted, optional, donor-local, or unresolved.
